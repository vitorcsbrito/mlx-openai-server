[project]
name = "mlx-openai-server"
version = "1.8.1"
description = "A high-performance API server that provides OpenAI-compatible endpoints for MLX models."
readme = "README.md"
requires-python = ">=3.11,<3.13"
license = { text = "MIT" }
authors = [{ name = "Gia-Huy Vuong", email = "cubist38@gmail.com" }]
keywords = [
  "mlx",
  "fastapi",
  "openai",
  "server",
  "api",
  "multimodal",
  "local models",
]
classifiers = [
  "Programming Language :: Python :: 3",
  "License :: OSI Approved :: MIT License",
  "Operating System :: OS Independent",
]

dependencies = [
  "aiofiles>=25,<26",
  "av>=16.0.1,<17",
  "click>=8.2.1,<8.4",
  "dill>=0.4,<0.5",
  "fastapi>=0.130,<0.142",
  "httpx>=0.28,<1",
  "json_repair>=0.52.1,<0.60",
  "librosa>=0.11.0,<0.12",
  "loguru>=0.7.3,<0.8",
  "mlx-embeddings>=0.0.5,<0.1",
  "mlx-lm>=0.31.3,<0.32",
  # >=0.6.2 for qwen3_5_moe_vision (Qwen3.5/3.6 MoE VL)
  "mlx-vlm>=0.6.8,<0.7",
  "mlx-whisper>=0.4.3,<0.5",
  "mlx>=0.32.0",
  "numpy>=2.2.0,<2.4",
  "openai>=2.21,<3",
  "openai-harmony>=0.0.8,<0.1",
  "outlines>=1.1.1,<1.2",
  "pillow>=12.1.1,<13.0",
  "pydantic>=2,<3",
  "PyYAML>=6.0,<7",
  "python-multipart>=0.0.20,<0.0.25",
  "rich>=14,<15",
  "torchvision>=0.23.0,<0.30",
  "uvicorn>=0.35.0,<0.40",
  "xxhash>=3.6,<4",
  "transformers>=5.14.0,<6"
]

# mflux dropped: it caps mlx<0.32, which mlx-vlm>=0.6.5 forbids. Image gen is
# already optional at runtime (app/server.py probes for it) — install separately.
[dependency-groups]
dev = [
  "codespell",
  "mypy",
  "pre-commit",
  "pytest",
  "pytest-asyncio",
  "ruff",
  "types-aiofiles",
  "types-cffi",
  "types-PyMySQL",
  "types-pyRFC3339",
  "types-python-dateutil",
  "types-PyYAML",
  "types-regex",
  "types-requests",
  "types-setuptools",
]
enhanced-caching = ["spacy", "regex", "tiktoken", "torch", "transformers"]

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]

[tool.setuptools.package-data]
app = ["py.typed"]

[project.urls]
Homepage = "https://github.com/cubist38/mlx-openai-server"

[project.scripts]
mlx-openai-server = "app.cli:cli"

[build-system]
requires = ["setuptools>=77.0"]
build-backend = "setuptools.build_meta"

[tool.uv]
prerelease = "allow"
# This is an Apple-Silicon MLX server; only resolve/lock for macOS. Without
# this, uv resolves for Linux too, where mflux pins mlx[cuda13]<=0.31.1 and
# conflicts with our mlx>=0.31.2 requirement (an unsatisfiable, irrelevant env).
environments = ["sys_platform == 'darwin'"]

[tool.codespell]
skip = "*.py,*.toml,*.ipynb"
quiet-level = 3

[tool.pytest.ini_options]
addopts = "--tb=short"
markers = [
  "integration: marks tests as integration tests that require external services",
]

[tool.ruff]
line-length = 100
indent-width = 4
fix = true
force-exclude = true
required-version = ">=0.12.0"
exclude = ["examples/"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "lf"
docstring-code-format = true
docstring-code-line-length = "dynamic"

[tool.ruff.lint]
select = [
  "A001",     # Variable {name} is shadowing a Python builtin
  "ANN001",   # Missing type annotation for function argument
  "ANN003",   # Missing type annotation for **kwargs
  "ANN201",   # Missing return type annotation for public function
  "ANN202",   # Missing return type annotation for private function
  "ANN204",   # Missing return type annotation for special method
  "ANN206",   # Missing return type annotation for classmethod
  "ASYNC210", # Async functions should not call blocking HTTP methods
  "ASYNC220", # Async functions should not create subprocesses with blocking methods
  "ASYNC221", # Async functions should not run processes with blocking methods
  "ASYNC222", # Async functions should not wait on processes with blocking methods
  "ASYNC230", # Async functions should not open files with blocking methods like open
  "ASYNC251", # Async functions should not call time.sleep
  "B002",     # Python does not support the unary prefix increment
  "B005",     # Using .strip() with multi-character strings is misleading
  "B007",     # Loop control variable {name} not used within loop body
  "B014",     # Exception handler with duplicate exception
  "B015",     # Pointless comparison. Did you mean to assign a value? Otherwise, prepend assert or remove it.
  "B017",     # pytest.raises(BaseException) should be considered evil
  "B018",     # Found useless attribute access. Either assign it to a variable or remove it.
  "B023",     # Function definition does not bind loop variable {name}
  "B026",     # Star-arg unpacking after a keyword argument is strongly discouraged
  "B032",     # Possible unintentional type annotation (using :). Did you mean to assign (using =)?
  "B904",     # Use raise from to specify exception cause
  "B905",     # zip() without an explicit strict= parameter
  #"BLE",
  "C",      # complexity
  "COM818", # Trailing comma on bare tuple prohibited
  "D",      # docstrings
  "DTZ003", # Use datetime.now(tz=) instead of datetime.utcnow()
  "DTZ004", # Use datetime.fromtimestamp(ts, tz=) instead of datetime.utcfromtimestamp(ts)
  "E",      # pycodestyle
  "F",      # pyflakes/autoflake
  "F541",   # f-string without any placeholders
  "FLY",    # flynt
  "FURB",   # refurb
  "G",      # flake8-logging-format
  "I",      # isort
  "INP",    # flake8-no-pep420
  "ISC",    # flake8-implicit-str-concat
  "ICN001", # import concentions; {name} should be imported as {asname}
  "LOG",    # flake8-logging
  "N804",   # First argument of a class method should be named cls
  "N805",   # First argument of a method should be named self
  "N815",   # Variable {name} in class scope should not be mixedCase
  "PERF",   # Perflint
  "PGH",    # pygrep-hooks
  "PIE",    # flake8-pie
  "PL",     # pylint
  "PT",     # flake8-pytest-style
  "PTH",    # flake8-pathlib
  "PYI",    # flake8-pyi
  "RET",    # flake8-return
  "RSE",    # flake8-raise
  "RUF005", # Consider iterable unpacking instead of concatenation
  #"RUF006", # Store a reference to the return value of asyncio.create_task
  "RUF010", # Use explicit conversion flag
  "RUF017", # Avoid quadratic list summation
  "RUF018", # Avoid assignment expressions in assert statements
  "RUF019", # Unnecessary key check before dictionary access
  # "RUF100", # Unused `noqa` directive; temporarily every now and then to clean them up
  "S102",  # Use of exec detected
  "S103",  # bad-file-permissions
  "S108",  # hardcoded-temp-file
  "S306",  # suspicious-mktemp-usage
  "S307",  # suspicious-eval-usage
  "S313",  # suspicious-xmlc-element-tree-usage
  "S314",  # suspicious-xml-element-tree-usage
  "S315",  # suspicious-xml-expat-reader-usage
  "S316",  # suspicious-xml-expat-builder-usage
  "S317",  # suspicious-xml-sax-usage
  "S318",  # suspicious-xml-mini-dom-usage
  "S319",  # suspicious-xml-pull-dom-usage
  "S601",  # paramiko-call
  "S602",  # subprocess-popen-with-shell-equals-true
  "S604",  # call-with-shell-equals-true
  "S608",  # hardcoded-sql-expression
  "S609",  # unix-command-wildcard-injection
  "SIM",   # flake8-simplify
  "SLF",   # flake8-self
  "SLOT",  # flake8-slots
  "T100",  # Trace found: {name} used
  "T20",   # flake8-print
  "TC",    # flake8-type-checking
  "TID",   # Tidy imports
  "TRY",   # tryceratops
  "UP",    # pyupgrade
  "UP031", # Use format specifiers instead of percent format
  "UP032", # Use f-string instead of `format` call
  "W",     # pycodestyle
]

ignore = [
  "ANN001", # Missing type annotation for function argument (TODO: fix incrementally)
  "ANN003", # Missing type annotation for **kwargs (TODO: fix incrementally)
  "ANN201", # Missing return type annotation for public function (TODO: fix incrementally)
  "ANN202", # Missing return type annotation for private function (TODO: fix incrementally)
  "ANN204", # Missing return type annotation for special method (TODO: fix incrementally)
  "ANN206", # Missing return type annotation for classmethod (TODO: fix incrementally)
  "ANN401", # Dynamically typed expressions (typing.Any) are allowed when necessary
  "D100",   # Missing docstring in public module (TODO: fix incrementally)
  "D101",   # Missing docstring in public class (TODO: fix incrementally)
  "D102",   # Missing docstring in public method (TODO: fix incrementally)
  "D103",   # Missing docstring in public function (TODO: fix incrementally)
  "D104",   # Missing docstring in public package (TODO: fix incrementally)
  "D105",   # Missing docstring in magic method (TODO: fix incrementally)
  "D200",   # One-line docstring should fit on one line (TODO: fix incrementally)
  "D202",   # No blank lines allowed after function docstring
  "D203",   # 1 blank line required before class docstring
  "D205",   # 1 blank line required between summary line and description (TODO: fix incrementally)
  "D213",   # Multi-line docstring summary should start at the second line
  "D400",   # First line should end with a period (TODO: fix incrementally)
  "D401",   # First line should be in imperative mood
  "D406",   # Section name should end with a newline
  "D407",   # Section name underlining
  "E402",   # Module level import not at top of file
  "E501",   # line too long

  "ASYNC230", # Async functions should not open files with blocking methods (TODO: fix incrementally)
  "B904",   # Use raise from to specify exception cause (TODO: fix incrementally)
  "PLC0415", # import should be at the top-level of a file (TODO: fix incrementally)
  "PLC1901", # {existing} can be simplified to {replacement} as an empty string is falsey; too many false positives
  "PLR0904", # Too many public methods
  "PLR0911", # Too many return statements ({returns} > {max_returns})
  "PLR0912", # Too many branches ({branches} > {max_branches})
  "PLR0913", # Too many arguments to function call ({c_args} > {max_args})
  "PLR0914", # Too many local variables
  "PLR0915", # Too many statements ({statements} > {max_statements})
  "PLR0916", # Too many Boolean expressions
  "PLR0917", # Too many positional arguments
  "PLR1702", # Too many nested blocks
  "PLR2004", # Magic value used in comparison, consider replacing {value} with a constant variable
  "PLW2901", # Outer {outer_kind} variable {name} overwritten by inner {inner_kind} target
  "PT011",   # pytest.raises({exception}) is too broad, set the `match` parameter or use a more specific exception
  "PT018",   # Assertion should be broken down into multiple parts
  "PTH",     # flake8-pathlib (TODO: migrate os.path to pathlib incrementally)
  "RUF001",  # String contains ambiguous unicode character.
  "RUF013",  # PEP 484 prohibits implicit Optional (TODO: fix incrementally)
  "RUF002",  # Docstring contains ambiguous unicode character.
  "RUF003",  # Comment contains ambiguous unicode character.
  "RUF015",  # Prefer next(...) over single element slice
  "SIM102",  # Use a single if statement instead of nested if statements
  "SIM103",  # Return the condition {condition} directly
  "SIM105",  # Use contextlib.suppress (TODO: fix incrementally)
  "SIM108",  # Use ternary operator {contents} instead of if-else-block
  "SIM115",  # Use context handler for opening files
  "T201",    # print found (TODO: replace with logging incrementally)
  "TRY004",  # Prefer TypeError exception for invalid type (TODO: fix incrementally)
  "TRY300",  # Consider moving this statement to an else block (TODO: fix incrementally)

  # Moving imports into type-checking blocks can mess with pytest.patch()
  "TC001",  # Move application import {} into a type-checking block
  "TC002",  # Move third-party import {} into a type-checking block
  "TC003",  # Move standard library import {} into a type-checking block
  "TID252", # Prefer absolute import over a relative import

  "TRY003", # Avoid specifying long messages outside the exception class
  "TRY301", # Abstract 'raise' to an inner function
  "TRY400", # Use `logging.exception` instead of `logging.error`

  "UP046", # Generic class uses `Generic` subclass instead of type parameters (Remove when minimum Python version is 3.12+)

  # May conflict with the formatter, https://docs.astral.sh/ruff/formatter/#conflicting-lint-rules
  "W191",
  "E111",
  "E114",
  "E117",
  "D206",
  "D300",
  "Q",
  "COM812",
  "COM819",
  "ISC001",

  # Disabled because ruff does not understand type of __all__ generated by a function
  "PLE0605",
]
fixable = ["ALL"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = [
  "SLF001", # Private member accessed
  #"D100", "D101", "D102", "D103", "D104", "D105", "D106", "D107",  # Docstring rules
  "PLR2004", # Magic value used in comparison
  "S101",    # Use of assert detected
  "PLR0913", # Too many arguments to function call
  "C901",    # Function is too complex
  "E501",    # line too long
  "B017",    # Don't assert blind Exception
  "PT006",   # Wrong type passed to first argument
  "S108",
  "D401",
  "D417",
]

[tool.ruff.lint.flake8-pytest-style]
fixture-parentheses = false
mark-parentheses = false

[tool.ruff.lint.isort]
force-sort-within-sections = true
combine-as-imports = true
split-on-trailing-comma = false

[tool.ruff.lint.mccabe]
max-complexity = 25

[tool.ruff.lint.pydocstyle]
property-decorators = ["propcache.cached_property"]
convention = "numpy"

[tool.mypy]
check_untyped_defs = true
disallow_any_generics = true
disallow_incomplete_defs = true
disallow_untyped_defs = true
no_implicit_optional = true
warn_redundant_casts = true
warn_return_any = true
warn_unused_configs = true
warn_unused_ignores = true
disable_error_code = ["import-untyped"]
files = ["app", "scripts"]
exclude = ["tests"]
python_executable = "./.venv/bin/python"

[[tool.mypy.overrides]]
module = ["dill.*", "mflux.*", "openai_harmony.*", "spacy.*"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = ["untyped_package.*"]
follow_untyped_imports = true
